import os
from Crypto.Util.Padding import pad
from pwn import xor


key = os.urandom(25)
flag = b"flag{????????????????????REDACTED???????????????????????}"
padenc = pad(flag,?)
enc = xor(padenc,key)
print(enc)
